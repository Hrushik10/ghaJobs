name: Mjobs
on: 
  push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is Build"
            echo "Hi"
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is Test"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is Deploy"
